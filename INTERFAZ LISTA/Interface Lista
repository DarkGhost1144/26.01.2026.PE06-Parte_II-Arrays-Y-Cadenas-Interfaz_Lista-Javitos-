/**
 * Interfaz que define las operaciones básicas de una lista numérica de enteros.
 * Permite insertar, eliminar, buscar elementos y obtener una representación en
 * forma de cadena.
 *
 * @author Jkur
 * @version 1.0
 * @see String
 */
public interface Lista {

    /**
     * Devuelve el número de elementos almacenados en la lista.
     *
     * @return tamaño actual de la lista
     */
    int getTamanyo();

    /**
     * Inserta un valor al final de la lista.
     *
     * @param valor número entero a insertar
     */
    void insertarAlFinal(int valor);

    /**
     * Inserta un valor al principio de la lista.
     *
     * @param valor número entero a insertar
     */
    void insertarAlPrincipio(int valor);

    /**
     * Inserta un valor en una posición concreta de la lista.
     *
     * @param posicion índice donde se insertará el valor
     * @param valor número entero a insertar
     */
    void insertarEnLaPosicion(int posicion, int valor);

    /**
     * Elimina el primer elemento de la lista.
     */
    void eliminarLaPrimera();

    /**
     * Elimina el último elemento de la lista.
     */
    void eliminarLaUltima();

    /**
     * Elimina el elemento situado en una posición concreta.
     *
     * @param posicion índice del elemento a eliminar
     */
    void eliminar(int posicion);

    /**
     * Busca un número dentro de la lista.
     *
     * @param n número a buscar
     * @return posición del número si se encuentra, o -1 si no existe
     */
    int buscar(int n);

    /**
     * Une la lista actual con otra lista pasada como parámetro.
     *
     * @param lista2 lista que se desea unir a la actual
     */
    void unirDosListas(Lista lista2);

    /**
     * Devuelve una representación en forma de cadena de la lista.
     *
     * @return cadena con los elementos de la lista
     */
    String imprimirLista();
}
